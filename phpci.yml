build_settings:
    verbose: true
    prefer_symlink: false
    ignore:
      - vendor

setup:
    env:
        APP_ENV: "test"
    composer:
        action: "install"
        prefer_dist: false
#    phing:
#        build_file: 'build.xml'
#        targets:
#          - "ci:run-tests"
#        properties:
#          config_file: "build.properties"

test:
    php_spec:
        args: ~
#    php_unit:
#        directory: "test/unit/"
#        args: "--bootstrap 'test/bootstrap.php'"
    php_mess_detector:
        allow_failures: true
    php_cpd:
        allow_failures: true
    php_code_sniffer:
        path: "apps"
#        ignore:
#            - "apps/views"
        standard: "PSR1,PSR2"

failure:
  email:
    committer: true
#    cc: ["php-ci@brzuchalski.com"]

#success:
#    phing:
#        build_file: 'build.xml'
#        targets:
#          - "deploy:test"
#        properties:
#          config_file: "build.properties"

